import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'car-itsur-laj',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
